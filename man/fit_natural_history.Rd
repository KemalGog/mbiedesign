% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_calibration.R
\name{fit_natural_history}
\alias{fit_natural_history}
\title{Calibrate the natural-history model to incidence data}
\usage{
fit_natural_history(
  OMST,
  LMST,
  cancer = "lung",
  k = 16,
  num_seeds = 1,
  mean1 = 3,
  risk_multiplier = 3.1,
  the_data = NULL
)
}
\arguments{
\item{OMST}{Target overall mean sojourn time, in years.}

\item{LMST}{Target late-stage mean sojourn time, in years.}

\item{cancer}{Cancer site whose incidence data to fit (currently
\code{"lung"}). Ignored when \code{the_data} is supplied.}

\item{k}{Number of states in the CTMC.}

\item{num_seeds}{Number of random optimizer restarts; the best fit is kept.}

\item{mean1}{Mean of the normal distribution used to draw starting values.}

\item{risk_multiplier}{Incidence inflation factor applied to the provided
data, e.g. \code{3.1} to reflect an NLST-eligible high-risk population. Ignored
when \code{the_data} is supplied.}

\item{the_data}{Optional incidence data frame (columns \code{midage}, \code{Count},
\code{Pop}, \code{years}, \code{stage}) to fit instead of the provided \code{cancer} data. Used
as-is, with no \code{risk_multiplier} applied.}
}
\value{
A list with the raw \code{optim} fit (\code{the_fit}), an optional diagnostic
plot (\code{outplot}, \code{NULL} if ggplot2/reshape2 are unavailable), the fitted
\code{rate.matrix}, and a \code{summary_out} list of sojourn-time quantities
(\code{OMST}, \code{LMST}, and the derived \code{EMST}, the early-stage mean sojourn time).
}
\description{
Fits the continuous-time multistate natural-history model of Lange et al.
(2024) to age- and stage-specific incidence data by maximum likelihood,
targeting a specified overall mean sojourn time (\code{OMST}) and late-stage mean
sojourn time (\code{LMST}). By default it loads and calibrates to the provided
incidence data for \code{cancer}; supply \code{the_data} to fit your own. The returned
object supplies the \code{rate.matrix} that the projection functions (e.g.
\code{\link[=get_trial_results_by_design]{get_trial_results_by_design()}}) consume. See the calibration vignette
(\code{vignette("calibration", package = "mbiedesign")}) for the data-prep
steps done internally.
}
\examples{
\dontrun{
# Fit the lung base case: loads provided incidence, applies the 3.1 multiplier
fit <- fit_natural_history(OMST = 4.0, LMST = 1.35)
fit$summary_out$EMST  # early-stage mean sojourn time (derived)
}
}
