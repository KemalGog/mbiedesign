% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/summary_outcomes_by_trial_design.R
\name{get_summary_outcomes_by_design}
\alias{get_summary_outcomes_by_design}
\title{Summary outcomes and sample size by trial design}
\usage{
get_summary_outcomes_by_design(
  rate_matrix,
  start_age,
  numscreens,
  screen_int,
  num_followup_intervals,
  sens_e,
  sens_l,
  specificity,
  design = c("traditional", "IE"),
  allocation_ratio = 1,
  alpha = 0.025,
  power = 0.9
)
}
\arguments{
\item{rate_matrix}{Natural-history transition rate matrix (e.g. from
\code{\link[=load_fitted_model]{load_fitted_model()}} or \code{\link[=fit_natural_history]{fit_natural_history()}}).}

\item{start_age}{Age at trial entry.}

\item{numscreens}{Number of screening examinations.}

\item{screen_int}{Interval between screens (years).}

\item{num_followup_intervals}{Follow-up after the final screen.}

\item{sens_e}{Early-stage test sensitivity.}

\item{sens_l}{Late-stage test sensitivity.}

\item{specificity}{Test specificity.}

\item{design}{Which design(s) to return: \code{"traditional"}, \code{"IE"}, or both.
Defaults to both.}

\item{allocation_ratio}{Screen-arm to control-arm allocation ratio
(n_screen / n_control); 1 is balanced (the default). Event rates are
per-participant, so only the allocation ratio affects the required sample
size — absolute arm sizes are not needed to estimate it.}

\item{alpha}{One-sided significance level.}

\item{power}{Target power (= 1 - beta).}
}
\value{
A data frame with one row per requested \code{design} and columns:
\describe{
\item{design}{\code{"traditional"} or \code{"IE"}.}
\item{p_c, p_s}{Control- and screen-arm late-stage event probabilities
(per randomized participant for traditional; per ever-positive for IE).}
\item{rr, rd}{Risk ratio (\code{p_s / p_c}) and risk difference (\code{p_c - p_s}).}
\item{fraction}{The subgroup fraction the design conditions on: \code{NA} for
traditional, the ever-positive fraction (pi) for IE.}
\item{n_per_arm}{Required per-arm sample size.}
\item{relative_efficiency}{N_traditional / N_design (1 for traditional).}
}
}
\description{
The main function of the package. Given a calibrated natural-history
\code{rate_matrix} and a screening schedule, it projects the end-of-trial
late-stage outcomes and computes the required per-arm sample size and
relative efficiency for the traditional and intended-effect (IE) designs,
returning one row per design (a summary table). For the full
time-dependent early/late/overall outcomes, use
\code{\link[=get_trial_results_by_design]{get_trial_results_by_design()}}.
}
