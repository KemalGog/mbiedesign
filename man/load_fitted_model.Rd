% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/load_fitted_model.R
\name{load_fitted_model}
\alias{load_fitted_model}
\title{Load a calibrated natural-history model}
\usage{
load_fitted_model(cancer = "lung", OMST = NULL, LMST = NULL, EMST = NULL)
}
\arguments{
\item{cancer}{Cancer site. Currently only \code{"lung"} is available.}

\item{OMST, EMST}{Overall (\code{OMST}) or early-stage (\code{EMST}) mean
sojourn time used to select a single model. Give exactly one of them,
together with \code{LMST}.}

\item{LMST}{Late-stage mean sojourn time; required to select a model.}
}
\value{
If no sojourn times are given, a list with:
\describe{
\item{base_fit}{The base-case model; use \code{base_fit$rate.matrix}.}
\item{base_omst, base_lmst, base_emst}{Base-case OMST (4.0, chosen), LMST
(1.35, chosen), and EMST (about 3.01, derived by the fit).}
\item{metadata}{Metadata for the sensitivity-analysis models (\code{fitID},
OMST, DMST).}
\item{fits}{List of calibrated models over the sensitivity analyses.}
\item{matched_pairs}{Lookup mapping a target EMST and LMST to the matched
OMST / \code{fitID}.}
}
If a model is selected, that single model object (with \code{rate.matrix} and
\code{summary_out}), as returned by \code{\link[=fit_natural_history]{fit_natural_history()}}.
}
\description{
Loads a lung cancer natural-history model calibrated by maximum likelihood to
SEER age- and stage-specific incidence (scaled by a risk multiplier of 3.1 to
reflect the elevated risk of an NLST-eligible population).
}
\details{
With no sojourn-time arguments, returns all the calibrated models (the
base-case model plus the sensitivity-analysis models and the lookup table).
Given \code{LMST} together with either \code{OMST} or \code{EMST}, returns a \strong{single}
calibrated model directly — the same object as \code{\link[=fit_natural_history]{fit_natural_history()}} — so
you can pass \code{model$rate.matrix} to the projection functions without
re-calibrating.

Two selection axes are supported because the models are organized that way:
the base case is defined by its chosen \code{OMST} and \code{LMST} (4.0, 1.35), while
the sensitivity-analysis models are organized by a target early-stage mean
sojourn time (\code{EMST}) and \code{LMST}, with \code{OMST} matched to reproduce that EMST.
Select the base case by \code{OMST} + \code{LMST}, and the sensitivity models by
\code{EMST} + \code{LMST}. Use \code{\link[=fit_natural_history]{fit_natural_history()}} for other combinations.
}
\examples{
# All calibrated models
all_models <- load_fitted_model("lung")
c(OMST = all_models$base_omst, LMST = all_models$base_lmst,
  EMST = all_models$base_emst)

# Select the base-case model directly (instant; no re-calibration)
model <- load_fitted_model("lung", OMST = 4.0, LMST = 1.35)
dim(model$rate.matrix)

# Select a sensitivity-analysis model by target EMST + LMST
m2 <- load_fitted_model("lung", EMST = 4.0, LMST = 1.35)
m2$summary_out$OMST   # the matched OMST
}
